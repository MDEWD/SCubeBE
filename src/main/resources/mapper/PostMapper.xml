<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.scube.scubebackend.mapper.PostMapper">
    
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.scube.scubebackend.model.entity.Post">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="title" property="title" />
        <result column="content" property="content" />
        <result column="vote_count" property="voteCount" />
        <result column="answer_count" property="answerCount" />
        <result column="view_count" property="viewCount" />
        <result column="accepted_answer_id" property="acceptedAnswerId" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="is_delete" property="isDelete" />
    </resultMap>
    
    <!-- 基础列 -->
    <sql id="Base_Column_List">
        id, user_id, title, content, vote_count, answer_count, view_count, 
        accepted_answer_id, create_time, update_time, is_delete
    </sql>
    
    <!-- 更新回答数 -->
    <update id="incrementAnswerCount">
        UPDATE post
        SET answer_count = answer_count + 1
        WHERE id = #{postId}
    </update>
    
</mapper>

